# 2026.269.1131 — docs(kind): add the skill triggers so the dispatcher routes to it

## Summary of changes

Add the `triggers:` line to the `kind` skill entity, so `charly marketplace
generate` emits a `DISPATCHER.md` row routing agents to the canonical `kind`
skill (the trigger mirrors the `plugin-kube` entity this skill was moved from in
PR #2).

| File | Change |
|---|---|
| `charly.yml` | the `kind-skill:` entity gains `triggers:` |

## How tested — manifest gate + live generation + the pre-merge limit, PROVEN

### The manifest gate

```
$ ./charly box validate
charly box validate: OK — checked 3 candies, 7 distros, 7 builders; 0 warnings, 0 errors
```

### The refs list resolves only CalVer tags (the load-bearing fact, proven)

Three generation runs against the same source, varying ONLY the refs-list pin for
`layer-kind`:

```
$ sed -i 's/<pin>/@github.com/opencharly/layer-kind:feat\/kind-triggers/' candy/charly-marketplace/charly.yml
$ charly -C <marketplace> marketplace generate --root <marketplace> --out <marketplace>
$ grep -c layer-kind <gen-branch.log>
0                                  # branch ref: NEVER resolved (layer-kind downloaded 0 times)

$ sed -i 's/<pin>/@github.com/opencharly/layer-kind:b7b976a7b376e1727594617ce39b4100400e321a/' …
$ charly … marketplace generate …
$ grep -c layer-kind <gen-sha.log>
0                                  # full-SHA ref: NEVER resolved either

$ sed -i 's/<pin>/@github.com/opencharly/layer-kind:v2026.269.0833/' …   # CalVer tag
$ charly … marketplace generate …
  kubernetes/skills/kind/SKILL.md  # tag ref: resolved; the entity rendered
```

So a PR head (branch or SHA) is skipped; only a CalVer tag resolves. **The tag
that would make this PR's entity resolvable is minted by this merge** — a genuine
chicken-and-egg, not a choice.

### What the tag-resolvable run emits, and what is invariant

The live tag-resolvable generation emitted the entity's `SKILL.md` and its
`index.json` entry:

```
$ sed -n '1,8p' kubernetes/skills/kind/SKILL.md
---
name: kind
description: |-
  The `kind` (Kubernetes-in-Docker) tool + per-engine host preconditions.
  Local Kubernetes clusters as node containers on the operator's engine
  (docker/podman/nerdctl via KIND_EXPERIMENTAL_PROVIDER). Use when composing
  the kind candy, the kindcluster substrate, or a local kind cluster.
---
$ python3 -c "import json; d=json.load(open('.well-known/skills/index.json')); print([s for s in (d if isinstance(d,list) else d.get('skills',[])) if s.get('name')=='kind'])"
[{"name": "kind", "files": ["SKILL.md"], "version": "3.1.0"}]
```

Both are **invariant to `triggers:`** — the `DISPATCHER.md` row is this diff's only
functional effect, and it can only be generated from a tag-resolvable ref.

### Maintainer sign-off for the pre-merge-unobservable portion

Per the validator's A2 escape, the maintainer of `opencharly/layer-kind` has signed
off on this thread (comment 5845749135) that the `DISPATCHER.md` row is a post-merge
effect, to be proven in the companion marketplace refs-bump PR that pins this
repo's new tag and pastes the real generated row. Summary of the sign-off:

> As the maintainer of `opencharly/layer-kind`, I authorize landing this PR with
> the `DISPATCHER.md` row proven **after** merge, in the companion marketplace
> refs-bump PR: that PR pins this repo's new tag, regenerates the corpus, and
> pastes the real generated `DISPATCHER.md` row as the generated-artifact proof.
> This is the family's existing mechanism (`layer-charly-kubernetes`'s `triggers:`
> → `/charly-kubernetes:check-k8s` and `…:helm` rows in `DISPATCHER.md`). No
> irregular pre-merge tag is created.

## Rulebook compliance

- **R0 (skills first):** loaded `/charly-internals:skills`,
  `/charly-internals:marketplace`, `/charly-image:layer`.
- **R1 (RCA, zero warnings):** the missing `DISPATCHER.md` row was root-caused to
  the absent `triggers:` field; the refs list's tag-only resolution was then
  established by measurement (three runs, pasted above), which is why the row is
  proven post-merge rather than faked here.
- **R2 (finish the cutover):** completed in the same phase as the skill's move; the
  linked marketplace PR closes the loop with the pasted row.
- **R3 (no duplication):** the single `kind` skill carries its own triggers.
- **R4 (no workaround):** the source entity is fixed; `DISPATCHER.md` is produced by
  the real generation, never hand-edited, and no irregular tag is minted.
- **R4a:** `N/A — no defect being routed around.`
- **R5 (sweep):** the entity is self-consistent with the family's other skills.
- **R6 (git safety):** session worktree; no direct push to `main`.
- **R7 (prove behavior):** the changed path is the entity → generated corpus; the
  refs-list mechanism (tag-only resolution) executed live and is pasted (three
  runs). The `triggers:` line's OWN effect (the `DISPATCHER.md` row) is not
  exercisable pre-merge and is **explicitly deferred under maintainer sign-off** —
  stated plainly, not claimed as executed.
- **R8 (emitted artifacts):** the emitted `SKILL.md` + `index.json` entry are
  pasted; the changed artifact (the dispatcher row) is invariant-elsewhere and its
  proof is the linked marketplace PR, authorized by the sign-off.
- **R9:** `N/A — no binary shipped; the candy is a manifest.`
- **R10:** `N/A — no runtime path changed`; the `kindcluster` substrate's runtime
  proof is charly#669's `check-kindcluster` R10 bed, and this manifest's gate is
  `box validate` + the live generation above.

## Change classification

- **Change class:** candy manifest (`charly.yml`) — the `triggers:` field of a
  `skill:` entity, whose only generated effect is the `DISPATCHER.md` row.
- **Verification gate:** `charly box validate` (0 warnings) + the refs-list
  resolution behavior measured live (three generation runs) + the entity's live
  generation of `SKILL.md`/`index.json`. The changed row is proven in the
  companion marketplace PR under maintainer sign-off.
- **Attribution tier:** `analysed on a live system` — the changed manifest and the
  generation mechanism ran live with retained output; the single deferred artifact
  is authorized by explicit maintainer sign-off and proven in the linked PR. The
  tier is not claimed above the evidence.

### Attribution evidence (A3)

The authoring runtime's own model registry reports this model as provider
`ollama-cloud`, id `ollama-cloud/deepseek-v4.1-flash`, name **`DeepSeek V4.1
Flash`**, status active. Harness: `OpenCode`.

*Assisted-by: OpenCode DeepSeek V4.1 Flash (analysed on a live system)*